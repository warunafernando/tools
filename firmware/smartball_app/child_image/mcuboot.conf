# MCUboot config for SmartBall A/B with rollback (§8)
CONFIG_BOOT_MAX_IMG_SLOTS=2
CONFIG_BOOT_UPGRADE_ONLY=n
CONFIG_BOOT_BOOTSTRAP=n

# Signed images only - set path in overlay or pass -D
# CONFIG_BOOT_SIGNATURE_TYPE_RSA=y
# CONFIG_BOOT_SIGNATURE_KEY_FILE="keys/smartball.pem"
